@model IEnumerable<QuanLyThueXe.Models.Reservation>
@using QuanLyThueXe.Helpers

@{
    ViewData["Title"] = "Quản lý đơn đặt xe";
}

<div class="container-fluid mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>📋 Quản lý đơn đặt xe</h2>
        <a asp-action="Create" class="btn btn-primary">
            ➕ Tạo đơn đặt mới
        </a>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            @TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <!-- Form tìm kiếm và lọc -->
    <div class="card mb-4 shadow">
        <div class="card-body">
            <form method="get" asp-action="Index" class="row g-3">
                <div class="col-md-8">
                    <label class="form-label">Tìm kiếm</label>
                    <input type="text" name="searchTerm" class="form-control" 
                           placeholder="Biển số, hãng xe, tên khách hàng, SĐT, Email..." 
                           value="@ViewBag.SearchTerm" />
                </div>
                <div class="col-md-3">
                    <label class="form-label">Trạng thái</label>
                    <select name="statusFilter" class="form-select">
                        <option value="All" selected="@(ViewBag.StatusFilter == "All" || ViewBag.StatusFilter == null)">Tất cả</option>
                        <option value="Pending" selected="@(ViewBag.StatusFilter == "Pending")">Chờ duyệt</option>
                        <option value="Approved" selected="@(ViewBag.StatusFilter == "Approved")">Đã duyệt</option>
                        <option value="Cancelled" selected="@(ViewBag.StatusFilter == "Cancelled")">Đã hủy</option>
                        <option value="Completed" selected="@(ViewBag.StatusFilter == "Completed")">Hoàn thành</option>
                    </select>
                </div>
                <div class="col-md-1 d-flex align-items-end">
                    <button type="submit" class="btn btn-primary w-100">🔍</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Thống kê -->
    <div class="row mb-4">
        <div class="col-md-3">
            <div class="card text-white bg-primary">
                <div class="card-body">
                    <h5 class="card-title">Tổng đơn</h5>
                    <h2 class="mb-0">@Model.Count()</h2>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card text-white bg-warning">
                <div class="card-body">
                    <h5 class="card-title">Chờ duyệt</h5>
                    <h2 class="mb-0">@Model.Count(r => r.Status == "Pending")</h2>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card text-white bg-success">
                <div class="card-body">
                    <h5 class="card-title">Đã duyệt</h5>
                    <h2 class="mb-0">@Model.Count(r => r.Status == "Approved")</h2>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card text-white bg-danger">
                <div class="card-body">
                    <h5 class="card-title">Đã hủy</h5>
                    <h2 class="mb-0">@Model.Count(r => r.Status == "Cancelled")</h2>
                </div>
            </div>
        </div>
    </div>

    <!-- Bảng danh sách đơn đặt -->
    <div class="card shadow">
        <div class="card-header bg-dark text-white">
            <h5 class="mb-0">Danh sách đơn đặt xe</h5>
        </div>
        <div class="card-body p-0">
            @if (Model != null && Model.Any())
            {
                <div class="table-responsive">
                    <table class="table table-hover mb-0">
                        <thead class="table-light">
                            <tr>
                                <th>Mã đơn</th>
                                <th>Xe</th>
                                <th>Khách hàng</th>
                                <th>Ngày đặt</th>
                                <th>Trạng thái</th>
                                <th>Thao tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var reservation in Model)
                            {
                                <tr>
                                    <td>
                                        <strong>#@reservation.ReservationId</strong>
                                    </td>
                                    <td>
                                        @if (reservation.Car != null)
                                        {
                                            <div>
                                                <strong>@reservation.Car.Brand @reservation.Car.Model</strong><br />
                                                <small class="text-muted">Biển số: @reservation.Car.LicensePlate</small><br />
                                                <span class="badge bg-info">@reservation.Car.VehicleType</span>
                                            </div>
                                        }
                                        else
                                        {
                                            <span class="text-muted">-</span>
                                        }
                                    </td>
                                    <td>
                                        @if (reservation.Customer != null)
                                        {
                                            <div>
                                                <strong>@reservation.Customer.FullName</strong><br />
                                                @if (!string.IsNullOrEmpty(reservation.Customer.Phone))
                                                {
                                                    <small class="text-muted">📞 @reservation.Customer.Phone</small>
                                                }
                                                @if (!string.IsNullOrEmpty(reservation.Customer.Email))
                                                {
                                                    <br /><small class="text-muted">📧 @reservation.Customer.Email</small>
                                                }
                                            </div>
                                        }
                                        else
                                        {
                                            <span class="text-muted">-</span>
                                        }
                                    </td>
                                    <td>
                                        @DateHelper.FormatDateTime(reservation.ReservationDate)
                                    </td>
                                    <td>
                                        @if (reservation.Status == "Pending")
                                        {
                                            <span class="badge bg-warning">⏳ Chờ duyệt</span>
                                        }
                                        else if (reservation.Status == "Approved")
                                        {
                                            <span class="badge bg-success">✅ Đã duyệt</span>
                                        }
                                        else if (reservation.Status == "Cancelled")
                                        {
                                            <span class="badge bg-danger">❌ Đã hủy</span>
                                        }
                                        else if (reservation.Status == "Completed")
                                        {
                                            <span class="badge bg-secondary">✓ Hoàn thành</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-info">@reservation.Status</span>
                                        }
                                    </td>
                                    <td>
                                        <div class="btn-group" role="group">
                                            <a asp-action="Details" asp-route-id="@reservation.ReservationId" 
                                               class="btn btn-sm btn-outline-primary" title="Xem chi tiết">
                                                👁️
                                            </a>
                                            @if (reservation.Status == "Pending")
                                            {
                                                <form asp-action="Approve" asp-route-id="@reservation.ReservationId" 
                                                      method="post" class="d-inline" 
                                                      onsubmit="return confirm('Bạn có chắc muốn duyệt đơn đặt này?');">
                                                    @Html.AntiForgeryToken()
                                                    <button type="submit" class="btn btn-sm btn-outline-success" title="Duyệt đơn">
                                                        ✅
                                                    </button>
                                                </form>
                                                <form asp-action="Cancel" asp-route-id="@reservation.ReservationId" 
                                                      method="post" class="d-inline" 
                                                      onsubmit="return confirm('Bạn có chắc muốn hủy đơn đặt này?');">
                                                    @Html.AntiForgeryToken()
                                                    <button type="submit" class="btn btn-sm btn-outline-danger" title="Hủy đơn">
                                                        ❌
                                                    </button>
                                                </form>
                                            }
                                            @if (reservation.Status != "Cancelled" && reservation.Status != "Completed")
                                            {
                                                <a asp-action="Edit" asp-route-id="@reservation.ReservationId" 
                                                   class="btn btn-sm btn-outline-warning" title="Sửa">
                                                    ✏️
                                                </a>
                                            }
                                            @if (reservation.Status == "Cancelled" || reservation.Status == "Completed")
                                            {
                                                <form asp-action="Delete" asp-route-id="@reservation.ReservationId" 
                                                      method="post" class="d-inline" 
                                                      onsubmit="return confirm('Bạn có chắc muốn xóa đơn đặt này?');">
                                                    @Html.AntiForgeryToken()
                                                    <button type="submit" class="btn btn-sm btn-outline-danger" title="Xóa">
                                                        🗑️
                                                    </button>
                                                </form>
                                            }
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="text-center py-5">
                    <div class="mb-3">
                        <span style="font-size: 4rem;">📭</span>
                    </div>
                    <h5>Chưa có đơn đặt xe nào</h5>
                    <p class="text-muted">Hãy tạo đơn đặt mới hoặc đợi khách hàng đặt xe.</p>
                    <a asp-action="Create" class="btn btn-primary">
                        ➕ Tạo đơn đặt mới
                    </a>
                </div>
            }
        </div>
    </div>

    <div class="mt-3">
        <a asp-controller="Home" asp-action="Index" class="btn btn-secondary">
            ⬅️ Quay lại trang chủ
        </a>
    </div>
</div>

